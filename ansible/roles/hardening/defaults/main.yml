# roles/hardening/defaults/main.yml
# SSH variables (ssh_port, ssh_permit_root_login, ssh_password_auth,
# ssh_pubkey_auth) are now defined in group_vars/all.yml.
# This file remains empty when no additional role-level defaults are needed.